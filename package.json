{
  "name": "big-yikes",
  "version": "1.0.0",
  "repository": "https://github.com/sventi555/big-yikes.git",
  "author": "Stephen Edwards <34760298+sventi555@users.noreply.github.com>",
  "license": "MIT",
  "scripts": {
    "client": "yarn --cwd ./packages/client",
    "lib": "yarn --cwd ./packages/lib",
    "server": "yarn --cwd ./packages/server",
    "dev": "concurrently -n \"lib,client,server\" \"yarn lib dev\" \"yarn client dev\" \"yarn server dev\"",
    "test": "yarn lib test && yarn client test && yarn server test",
    "lint": "yarn lib lint && yarn client lint && yarn server lint",
    "postinstall": "yarn lib; yarn client; yarn server"
  },
  "devDependencies": {
    "concurrently": "^7.2.2"
  }
}
