{
  "name": "big-yikes",
  "version": "1.0.0",
  "repository": "https://github.com/sventi555/big-yikes.git",
  "author": "Stephen Edwards <34760298+sventi555@users.noreply.github.com>",
  "license": "MIT",
  "scripts": {
    "client": "yarn --cwd ./packages/client",
    "lib": "yarn --cwd ./packages/lib",
    "server": "yarn --cwd ./packages/server",
    "dev": "concurrently -n \"client,lib\" \"yarn client dev\" \"yarn lib dev\"",
    "lint": "yarn client lint && yarn lib lint",
    "postinstall": "yarn client; yarn lib"
  },
  "devDependencies": {
    "concurrently": "^7.2.2"
  }
}
